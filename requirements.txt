# Gemini legacy mode (modern google.genai path intentionally excluded)
google-generativeai>=0.8.0
# Optional: Vertex AI Gemini (provider=vertex); uses ADC / service account
google-cloud-aiplatform>=1.70.0

flask
flask-cors
requests>=2.28.0
html2text>=2020.1.16
markdown
lancedb
pymupdf
python-dotenv
